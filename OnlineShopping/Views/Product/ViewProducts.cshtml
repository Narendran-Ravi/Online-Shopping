@model IEnumerable<OnlineShopping.DomainModel.Producttable>
    
@{
        ViewBag.Title = "Products List";
        Layout = "~/Views/Shared/_Admin.cshtml";
    }

    <table class="table table-hover">
        <tr>
            <th>Id</th>

            <th>Category</th>

            <th>Product Name</th>

            <th>Brand</th>

            <th>Price</th>

            <th>Description</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ProductID</td>

                <td>@item.Category</td>

                <td>@item.ProductName</td>

                <td>@item.Brand</td>

                <td>@item.Price</td>

                <td>@item.Description</td>
                <td>
                    <a href="@Url.Action("Update",new { id=item.ProductID})" class="btn btn-success">Edit</a>
                    <a href="@Url.Action("DeleteProduct",new{ id=item.ProductID })" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </table>